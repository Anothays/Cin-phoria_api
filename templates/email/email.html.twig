<!doctype html>
<html lang="fr">
<head>
</head>
<body>
<div class="body">
    <div class="container">
        <div class="content">
            <h1 class="text-center">Cliquez sur le lien ci-dessous pour réinitialiser votre mot de passe</h1>
            <a class="block" href="{{ base_front_url }}/forgot-password/reset/{{ resetToken.token }}">
                {{ base_front_url }}/forgot-password/reset/{{ resetToken.token }}
            </a>
        </div>
    </div>
</div>
</body>
</html>